import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import { gunzipSync } from 'node:zlib';

const root=path.resolve(import.meta.dirname,'..');
const docs=path.join(root,'docs');
const index=fs.readFileSync(path.join(docs,'index.html'),'utf8');
assert.match(index,/<!doctype html>/i,'index must be valid HTML');
assert.match(index,/bootstrap-v719\.js/,'index must use the external v7.1.9 bootstrap');
assert.match(index,/Atrangi Document Workspace v7\.1\.9/,'index title must be v7.1.9');
for(const forbidden of ['DecompressionStream','document.write(text)','const text=source.replace','hardCss=document.createElement'])assert.ok(!index.includes(forbidden),`bootstrap implementation must not be inline in index: ${forbidden}`);
const inline=[...index.matchAll(/<script(?![^>]+src=)[^>]*>([\s\S]*?)<\/script>/gi)].map(m=>m[1].trim()).filter(Boolean);
assert.equal(inline.length,0,'index must contain no inline JavaScript');
const bootstrap=fs.readFileSync(path.join(docs,'bootstrap-v719.js'),'utf8');
assert.doesNotThrow(()=>new Function(bootstrap),'external bootstrap JS must parse');
for(const token of ["RELEASE='719'","VERSION='7.1.9'",'DecompressionStream','branding-v719.css','branding-v719.js','brandHomeBtn','themeToggleBtn'])assert.ok(bootstrap.includes(token),`external bootstrap missing ${token}`);
const chunks=Array.from({length:7},(_,i)=>`app-${String(i).padStart(2,'0')}.txt`);
const encoded=chunks.map(name=>{const f=path.join(docs,name);assert.ok(fs.existsSync(f),`missing chunk ${name}`);return fs.readFileSync(f,'utf8').trim()}).join('');
const app=gunzipSync(Buffer.from(encoded,'base64')).toString('utf8');
assert.match(app,/<!doctype html>/i,'decoded bundle must be HTML');
for(const token of ['class="topbar"','class="brand-block"','class="brand-logo"','class="top-actions"','id="homePanel"','id="desktopNav"','id="mobileNav"','id="heroScanBtn"','id="heroImportBtn"','id="heroWorkspaceBtn"','id="universalImportInput"'])assert.ok(app.includes(token),`missing base UI contract ${token}`);
const refs=[...app.matchAll(/<(?:link|script)[^>]+(?:href|src)="([^"]+)"[^>]*>/gi)].map(m=>m[1]);
const local=refs.filter(u=>!/^(?:[a-z]+:|\/\/|#|data:)/i.test(u)).map(u=>u.split(/[?#]/,1)[0]);
for(const asset of new Set(local))assert.ok(fs.existsSync(path.join(docs,asset)),`missing base asset ${asset}`);
for(const asset of ['bootstrap-v719.js','branding-v719.css','branding-v719.js','atrangi-brand-logo.svg','atrangi-brand-logo.b64','hardening-v715.css','hardening-v715.js'])assert.ok(fs.existsSync(path.join(docs,asset)),`missing release asset ${asset}`);
const version=JSON.parse(fs.readFileSync(path.join(docs,'version.json'),'utf8'));assert.equal(version.webVersion,'7.1.9','version.json must be v7.1.9');
const branding=fs.readFileSync(path.join(docs,'branding-v719.js'),'utf8');assert.doesNotThrow(()=>new Function(branding),'branding JS must parse');
for(const token of ["VERSION='7.1.9'",'themeToggleBtn','brandingTest','brandingFunctionalTest','brandHomeBtn','applyApprovedLogo','atrangi-brand-logo.b64'])assert.ok(branding.includes(token),`branding runtime missing ${token}`);
const b64=fs.readFileSync(path.join(docs,'atrangi-brand-logo.b64'),'utf8').replace(/\s+/g,'');assert.ok(b64.length>1000,'approved logo base64 must be populated');
const sw=fs.readFileSync(path.join(docs,'sw.js'),'utf8');assert.doesNotThrow(()=>new Function(sw),'service worker JS must parse');assert.match(sw,/atrangi-document-workspace-v7\.1\.9/,'service worker cache must be v7.1.9');
for(const asset of ['bootstrap-v719.js','branding-v719.js','branding-v719.css','atrangi-brand-logo.b64'])assert.ok(sw.includes(asset),`service worker missing ${asset}`);
console.log(`Verified v7.1.9 external-bootstrap package: ${chunks.length} chunks, ${new Set(local).size} bundled assets, no inline startup JavaScript.`);
