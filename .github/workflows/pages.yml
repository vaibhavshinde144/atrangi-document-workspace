name: Validate and Deploy Atrangi Workspace

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'scripts/**'
      - 'app/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle.properties'
      - '.github/workflows/pages.yml'

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: atrangi-pages
  cancel-in-progress: false

jobs:
  validate-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Validate exact deployable package
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ci
          node scripts/verify-web-package.mjs
          while IFS= read -r -d '' file; do
            echo "Syntax: $file"
            node --check "$file"
          done < <(find docs -maxdepth 1 -type f -name '*.js' -print0 | sort -z)
          python scripts/runtime-diagnostics.py --report ci/runtime-diagnostics.json
          python - <<'PY'
          from pathlib import Path
          html=Path('docs/index.html').read_text(encoding='utf-8')
          forbidden=['DecompressionStream','document.write(text)','const text=source.replace','hardCss=document.createElement']
          bad=[x for x in forbidden if x in html]
          assert not bad, f'inline bootstrap tokens in deployable index: {bad}'
          assert 'bootstrap-v720.js' in html, 'external v7.2.0 bootstrap missing'
          bootstrap=Path('docs/bootstrap-v720.js').read_text(encoding='utf-8')
          for token in ['shareAppBtn','Share / Install App','branding-v720.js']:
              assert token in bootstrap, token
          print('External bootstrap + Share App WebView guard passed')
          PY

      - name: Install Selenium
        run: python -m pip install --disable-pip-version-check selenium

      - name: Run real browser acceptance suite
        shell: bash
        run: |
          set -euo pipefail
          python -m http.server 8123 --directory docs >ci/http.log 2>&1 &
          PID=$!
          trap 'kill $PID 2>/dev/null || true' EXIT
          sleep 2
          python scripts/browser-acceptance.py http://127.0.0.1:8123/

      - name: Build Android v7.2.0 APK
        shell: bash
        run: |
          set -euo pipefail
          gradle :app:assembleDebug --no-daemon --stacktrace
          APK=app/build/outputs/apk/debug/app-debug.apk
          test -s "$APK"
          unzip -t "$APK" >/dev/null
          unzip -l "$APK" | grep -q 'atrangi_riders_launcher' || { echo 'Atrangi Riders launcher resource missing from APK'; exit 1; }

      - name: Prepare public install package
        shell: bash
        run: |
          set -euo pipefail
          APK=app/build/outputs/apk/debug/app-debug.apk
          mkdir -p docs/downloads
          cp "$APK" docs/downloads/Atrangi-Document-Workspace.apk
          cp "$APK" docs/downloads/Atrangi-Document-Workspace-v7.2.0.apk
          sha256sum docs/downloads/Atrangi-Document-Workspace.apk | awk '{print $1}' > docs/downloads/Atrangi-Document-Workspace.sha256
          python - <<'PY'
          import hashlib,json
          from pathlib import Path
          p=Path('docs/downloads/Atrangi-Document-Workspace.apk')
          result={
            'version':'7.2.0',
            'versionCode':5,
            'apkUrl':'https://vaibhavshinde144.github.io/atrangi-document-workspace/downloads/Atrangi-Document-Workspace.apk',
            'bytes':p.stat().st_size,
            'sha256':hashlib.sha256(p.read_bytes()).hexdigest()
          }
          Path('docs/downloads/latest.json').write_text(json.dumps(result,indent=2)+'\n')
          print(json.dumps(result,indent=2))
          PY
          test -s docs/downloads/Atrangi-Document-Workspace.apk

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Upload validation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: atrangi-v720-validation
          path: |
            ci/
            app/build/outputs/apk/debug/app-debug.apk
            docs/downloads/latest.json
            docs/downloads/Atrangi-Document-Workspace.sha256
          if-no-files-found: ignore
          retention-days: 14

      - name: Record validation and deployment result
        if: always()
        shell: bash
        env:
          DEPLOY_STATUS: ${{ job.status }}
          SOURCE_COMMIT: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          DEPLOYED_URL: ${{ steps.deployment.outputs.page_url }}
        run: |
          set -euo pipefail
          mkdir -p ci
          export RECORDED_AT_UTC="$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          python - <<'PY'
          import json,os
          from pathlib import Path
          result={
            'status':os.environ.get('DEPLOY_STATUS','unknown'),
            'sourceCommit':os.environ.get('SOURCE_COMMIT',''),
            'workflowRun':os.environ.get('RUN_ID',''),
            'deployedUrl':os.environ.get('DEPLOYED_URL',''),
            'webVersion':'7.2.0',
            'nativeVersionCode':5,
            'nativeVersionName':'7.2.0',
            'deliveryMode':'external-bootstrap+public-apk',
            'apkUrl':'https://vaibhavshinde144.github.io/atrangi-document-workspace/downloads/Atrangi-Document-Workspace.apk',
            'recordedAtUtc':os.environ.get('RECORDED_AT_UTC','')
          }
          Path('ci/pages-deploy-status.json').write_text(json.dumps(result,indent=2)+'\n')
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add ci/pages-deploy-status.json
          test ! -f ci/runtime-diagnostics.json || git add ci/runtime-diagnostics.json
          test ! -f ci/browser-acceptance.json || git add ci/browser-acceptance.json
          git commit -m 'CI: record v7.2.0 launcher/share/install acceptance result' || exit 0
          git pull --rebase origin main
          git push origin HEAD:main
